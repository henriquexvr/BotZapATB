RIOT_API_KEY=
GEMINI_API_KEY=
WHATSAPP_TARGET=
POLLING_INTERVAL=600000
PORT=3000
NODE_ENV=production

# Opcional: força o caminho da pasta de persistência.
# Se vazio ou nao definido, usa <pasta do projeto>/persist.
# Use isto se quiser manter os dados fora da pasta do projeto (ex: /var/lib/...).
# PERSIST_PATH=/var/lib/atb-lol-bot/persist
